apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  labels:
    kubevirt-vm: vm-{{.name}}-{{.Replica}}
    test-type: nic-hotplug-vlan
{{- range $key, $value := .vmLabels }}
    {{ $key }}: {{ $value }}
{{- end }}
  name: {{.name}}-{{.Replica}}
spec:
  runStrategy: Always
  template:
    metadata:
      labels:
        kubevirt-vm: vm-{{.name}}-{{.Replica}}
        kubevirt.io/os: windows
        test-type: nic-hotplug-vlan
{{- range $key, $value := .vmLabels }}
        {{ $key }}: {{ $value }}
{{- end }}
    spec:
      accessCredentials:
      - sshPublicKey:
          propagationMethod:
            qemuGuestAgent:
              users:
                - "{{ .vmUser }}"
          source:
            secret:
              secretName: "{{ .sshPublicKeySecret }}-{{ .counter }}"
      {{- if .nodeSelector }}
      {{- $nsParts := splitList "=" .nodeSelector }}
      nodeSelector:
        {{ index $nsParts 0 | quote }}: {{ index $nsParts 1 | quote }}
      {{- end }}
      domain:
        clock:
          timer:
            hpet:
              present: false
            hyperv: {}
            pit:
              tickPolicy: delay
            rtc:
              tickPolicy: catchup
        cpu:
          cores: {{.cpuCores}}
        devices:
          disks:
          - disk:
              bus: virtio
            name: rootdisk
          interfaces:
          - masquerade: {}
            model: virtio
            name: default
          {{- range $i := until (.nicCount | int) }}
          - bridge: {}
            model: virtio
            name: test-network-vlan-{{$i}}
          {{- end }}
          networkInterfaceMultiqueue: true
          rng: {}
          tpm: {}
        features:
          acpi: {}
          apic: {}
          smm:
            enabled: true
          hyperv:
            frequencies: {}
            ipi: {}
            reenlightenment: {}
            relaxed: {}
            reset: {}
            spinlocks:
              spinlocks: 8191
            synic: {}
            tlbflush: {}
            vapic: {}
            vpindex: {}
        firmware:
          bootloader:
            efi: {}
        machine:
          type: q35
        resources:
          requests:
            memory: {{.memory}}
      networks:
      - name: default
        pod: {}
      {{- range $i := until (.nicCount | int) }}
      - name: test-network-vlan-{{$i}}
        multus:
          networkName: test-network-vlan-{{add $i 1}}
      {{- end }}
      volumes:
      - dataVolume:
          name: dvclone-vlan-{{.Replica}}
        name: rootdisk
  dataVolumeTemplates:
  - metadata:
      name: dvclone-vlan-{{.Replica}}
    spec:
      source:
{{- if hasPrefix "docker://" .registryUrl }}
        registry:
          url: {{ .registryUrl }}
{{- else if hasPrefix "pvc://" .registryUrl }}
{{- $pvcParts := splitList "/" (trimPrefix "pvc://" .registryUrl) }}
        pvc:
          namespace: {{ index $pvcParts 0 }}
          name: {{ index $pvcParts 1 }}
{{- else }}
        http:
          url: {{ .registryUrl }}
{{- end }}
      storage:
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: {{.storage}}
        storageClassName: {{.storageclass}}
        volumeMode: {{.volumemode}}
